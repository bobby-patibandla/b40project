--- # Install Apache and Start service without Handler
- hosts: web
  become: true
  tasks:
    - yum:
        name:
          - httpd
        state: present
    - service: name=httpd state=started enabled=yes
    - raw: "echo Welcome to Rise 'n' Shine | sudo tee /var/www/html/index.html"
